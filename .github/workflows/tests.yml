name: tests

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:

  xcode-project-test:
    runs-on: macos-15
    strategy:
      fail-fast: false
      matrix:
        # Check the Github runner images when updating this matrix: https://github.com/actions/runner-images/tree/main/images/macos
        flags: [
          "-scheme AppAuth-iOS -destination 'platform=iOS Simulator,name=iPhone 16,OS=18.6' -sdk 'iphonesimulator'",
          "-scheme AppAuth-macOS -destination 'platform=macOS,arch=x86_64' -sdk 'macosx'",
          "-scheme AppAuth_macOS -destination 'platform=macOS,arch=x86_64' -sdk 'macosx'",
          "-scheme AppAuth-tvOS -destination 'platform=tvOS Simulator,name=Apple TV,OS=18.5' -sdk 'appletvsimulator'",
          "-scheme AppAuth_tvOS -destination 'platform=tvOS Simulator,name=Apple TV,OS=18.5' -sdk 'appletvsimulator'",
          "-scheme AppAuthTV -destination 'platform=tvOS Simulator,name=Apple TV,OS=18.5' -sdk 'appletvsimulator'"
        ]
    steps:
    - uses: actions/checkout@v5
    - name: Select Xcode
      run: sudo xcode-select -s /Applications/Xcode_16.4.app/Contents/Developer
    - name: Warm up simulator runtimes
      run: xcrun simctl list devices available
    - name: Run unit test targets
      run: |
        xcodebuild test \
          -project AppAuth.xcodeproj \
          ${{ matrix.flags }}

  pod-lib-lint:
    runs-on: macos-15
    strategy:
      matrix:
        flags: [
          '',
          '--use-libraries',
          '--use-static-frameworks'
        ]
    steps:
    - uses: actions/checkout@v5
    - name: Update Bundler
      run: bundle update --bundler
    - name: Install Ruby gems with Bundler
      run: bundle install
    - name: Lint podspec using local source
      run: pod lib lint --verbose ${{ matrix.flags }}

  spm-build-test:
    runs-on: macos-15
    steps:
    - uses: actions/checkout@v5
    - name: Build unit test target
      run: swift build
    - name: Run unit test target
      run: swift test --enable-code-coverage
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v6
      with: 
        version: v0.7.3

  xcode-27-preview-test:
    # Xcode 27 is currently only available on the preview `xcode-27` image,
    # on which Xcode 27 beta is the only (thus, default) Xcode and the simulator
    # runtimes are the 27.0 generation. The image is preview, so this job is does
    # not block merges.
    # https://github.com/actions/runner-images/issues/14404
    runs-on: xcode-27
    continue-on-error: true
    strategy:
      fail-fast: false
      matrix:
        flags: [
          "-scheme AppAuth-iOS -destination 'platform=iOS Simulator,name=iPhone 17,OS=27.0' -sdk 'iphonesimulator'",
          "-scheme AppAuth-macOS -destination 'platform=macOS' -sdk 'macosx'",
          "-scheme AppAuth_macOS -destination 'platform=macOS' -sdk 'macosx'",
          "-scheme AppAuth-tvOS -destination 'platform=tvOS Simulator,name=Apple TV,OS=27.0' -sdk 'appletvsimulator'",
          "-scheme AppAuth_tvOS -destination 'platform=tvOS Simulator,name=Apple TV,OS=27.0' -sdk 'appletvsimulator'",
          "-scheme AppAuthTV -destination 'platform=tvOS Simulator,name=Apple TV,OS=27.0' -sdk 'appletvsimulator'"
        ]
    steps:
    - uses: actions/checkout@v5
    - name: Warm up simulator runtimes
      run: xcrun simctl list devices available
    - name: Run unit test targets
      run: |
        xcodebuild test \
          -project AppAuth.xcodeproj \
          ${{ matrix.flags }}
